// tslint:disable-next-line: class-name
export interface fireStoreInterface {
    id: string; // este es el id del documento que se encuentra en la coleccion
    fotoUrl: string;
    desc: string;
    titulo: string;
    video: string;
}
